#include<stdio.h>
int main()
{
    double pi,volume;
    int r;
    scanf("%d",&r);
    pi = 3.14159;
    (double)r;
    volume = (4.0/3) * pi * r*r*r;
   printf("VOLUME = %.3lf",volume) ;
   return 0;
}
