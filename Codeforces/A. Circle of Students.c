#include<stdio.h>
int main()
{
    int i,j,k,q,n,p;
    scanf("%d",&q);
    for(i = 1; i <= q; i++)
    {
        scanf("%d",&n);
        for(j = 1; j <= n; j++)
        {
            scanf("%d",&p);
        }
    }
    return 0;
}
